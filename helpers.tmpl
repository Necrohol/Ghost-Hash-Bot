// Code generated by Phantom-Bot; DO NOT EDIT.
package main

import (
	"fmt"
	"strings"
)

func init() {
	// Registry bind for {{.Eclass}}
	EclassRegistry["{{.Eclass}}"] = func(pv, pn string) (string, string) {
		url := "{{.URLTemplate}}"
		rename := "{{.RenameTemplate}}"
		
		// K.I.S.S. variable replacement
		r := strings.NewReplacer(
			"${PV}", pv,
			"${PN}", pn,
			"${P}",  pn+"-"+pv,
		)
		
		return r.Replace(url), r.Replace(rename)
	}
}
